<footer class="container blog-footer px-4 bg-dark-blue">
  <div class="row">
    <div class="col-12 col-md-6">
      <h3 class="h4 text-white">Kontakt</h3>
      <p>
        ...
      </p>
    </div>
    <nav class="col-12 col-md-6">
      <ul>
        {% for item in menu.items %}
          <li><a class="text-light" href="{{ item.link }}">{{ item.title }}</a></li>
        {% endfor %}
      </ul>
    </nav>
  </div>
</footer>
{{ wp_footer }}
